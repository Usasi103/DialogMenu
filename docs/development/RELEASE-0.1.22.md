# DialogMenu 0.1.22 正式版验证

日期：2026-09-26。正式版以此前已验证的累计预发布为基线，重新构建正式版本号。

- 构建与源码格式检查通过，106 项测试通过，0 失败。
- 逐项比较正式 JAR 与同分支预发布 JAR：只有 `plugin.yml` 的版本信息变化，所有 class 与内置资源包字节保持一致。
- 使用正式 JAR 在独立 Paper 26.2 build 123 + CraftEngine 26.9.1 实例复测：真实 CE 图片、默认和显式图片标签、多语言选择与回退、配置检查、重载前后探针通过。
- JAR SHA-256：`dad36a0f4de8b0a323b52f49a3e13681fd566a9d7c8441f74feaa17a75abbeb4`。
- 此前用户 Minecraft 26.2 截图已确认中文翻译、玩家名参数、显式 CE 与默认图片显示正常。真实 IA 联动与客户端切换语言后的画面仍待实测；此项限制单独记录，不改变正式版发布状态。
